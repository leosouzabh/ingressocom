pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                sh 'echo checkout from a TAG'
            }
        }
        stage('build') {
            steps {
                sh 'ls'
            }
        }
    }
}
