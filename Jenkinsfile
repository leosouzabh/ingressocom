pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                sh 'echo checkout from MAIN branch'
            }
        }
        stage('build') {
            steps {
                sh 'ls'
            }
        }
    }
}
